spring:
  datasource:
    url: jdbc:mysql://localhost/jpa
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
#     更新或者创建数据表结构  DML() DCL DDL DQL  (create table )
      ddl-auto: update
#    控制台显示SQL
    show-sql: true
  thymeleaf:
    mode: LEGACYHTML5
    cache: false
  mvc:
    date-format: yyyy-MM-dd
server:
  port: 9090
